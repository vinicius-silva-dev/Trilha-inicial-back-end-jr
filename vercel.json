{
  "version": 2,
  "builds": {
    "env": {
      "DATABASE_URL": "file:./dev.db"
    },
    "commands": {
      "build": "prisma generate && next build"
    }

  },
    "routes": [
    { "src": "/(.*)", "dest": "src/app.js" }
  ]
}
